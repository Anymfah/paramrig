/** Pointer target for grab handles. Keep in sync with `--hit-target` in tokens.css. */
export const HIT_TARGET_PX = 32

/** Coarse / touch pointer target. Keep in sync with `--hit-target-coarse`. */
export const HIT_TARGET_COARSE_PX = 44
